pipeline {
    agent any

    stages {
        stage('One') {
            steps {
              script{
                def a = 5; 
             //def b = "${params.inputvalue}";
             c = "${a + b}" ;
             
             echo "value of c is  ${c}"
            }
        }
    }
}
}  
